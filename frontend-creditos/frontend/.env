# Vacío a propósito: las peticiones van a rutas relativas (ej. /auth/login)
# y Vite las reenvía al backend según el "proxy" definido en vite.config.js.
# Esto evita el bloqueo de CORS sin tener que tocar el backend.
VITE_API_URL=
